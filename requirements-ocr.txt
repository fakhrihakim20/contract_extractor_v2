# Optional PaddleOCR runtime dependencies.
# Streamlit Cloud free tier can fail while installing these large packages.
# Keep the main app installable through requirements.txt; install this file
# only in an environment that has enough build/install budget for PaddleOCR.
paddlepaddle==3.3.1
paddleocr==3.3.3
